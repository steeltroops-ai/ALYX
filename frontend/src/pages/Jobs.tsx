import React from 'react';
import { Typography, Box } from '@mui/material';

const Jobs: React.FC = () => {
    return (
        <Box>
            <Typography variant="h4" gutterBottom>
                Analysis Jobs
            </Typography>
            <Typography variant="body1" color="text.secondary">
                Job management interface will be implemented in future tasks.
            </Typography>
        </Box>
    );
};

export default Jobs;